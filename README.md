# Mobile Store

A React Router mobile-store application with product listing and details pages plus an admin panel for adding, editing, and deleting products.

## Run

```bash
npm install
npm start
```

Open http://localhost:8080. Product details use `/products/:id`; inventory management is available at `/admin`.
