import React from 'react'; import { Link } from 'react-router-dom';
export default function ProductList({ products }) { return <main className="content"><h1>Available Mobiles</h1><p className="lead">Find the phone that fits your life.</p><section className="product-grid">{products.map((product) => <article className="product-card" key={product.id}><img src={product.image} alt={product.name} /><div><Link className="product-link" to={`/products/${product.id}`}>{product.name}</Link><p>Price: ₹{product.price}</p><Link className="btn" to={`/products/${product.id}`}>View details</Link></div></article>)}</section></main>; }
